class Config:
    # Config de BD (ajústala si cambias credenciales)
    DB = dict(
        host="127.0.0.1",
        port=3306,
        user="root",
        password="123",
        database="registro_comidas",
    )

    # Otras configs futuras: SECRET_KEY, etc.
